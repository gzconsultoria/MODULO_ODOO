<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_finance_document_expiration" model="ir.cron">
        <field name="name">Financeiro: monitorar documentos vencidos</field>
        <field name="model_id" ref="finance_compliance.model_finance_document"/>
        <field name="state">code</field>
        <field name="code">model.search([])._compute_is_expired()</field>
        <field name="interval_type">days</field>
        <field name="interval_number">1</field>
        <field name="numbercall">-1</field>
    </record>
</odoo>
